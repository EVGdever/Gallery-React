
/**
 * Отступы в картинках в px
 * @type {number}
 */
const IMG_MARGIN = 5;

/**
 * Минимальное кол-во картинок в ряду
 * @type {number}
 */
const MIN_IMG_COUNT = 4;

/**
 * Максимальная ширина строки в px
 * @type {number}
 */
const MAX_ROW_WIGHT = 860;

/**
 * Максимальная высота строки в px
 * @type {number}
 */
const MAX_ROW_HEIGHT = 200;

module.exports = {
    IMG_MARGIN,
    MIN_IMG_COUNT,
    MAX_ROW_WIGHT,
    MAX_ROW_HEIGHT,
}
